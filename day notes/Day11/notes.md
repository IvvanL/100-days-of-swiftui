# Day 11 Notes - STRUCTS PART 2 COMPLETE

## FINISHED FIRST SECTION OF DAY 11 - Structs part 2
- How to limit access to internal data using different access control methods
- why access control is very important and how it affects your code
- took a small test on access control

## FINISHED STRUCTS PART 2
- Created custom structs using the struct keyword, with their own properties and methods
- Learned that methods which modify struct properties must be marked as mutating
- Understood the difference between stored properties (hold a value) and computed properties (calculate a value on the fly)
- Used willSet and didSet property observers to automatically run code when a property changes
- Swift auto-generates a memberwise initializer for free, but you can override it with a custom init
- Used access control (private) to protect properties and methods from outside code
- Learned that static properties and methods belong to the type itself, not to any individual instance — useful for shared data like counters or app-wide constants
